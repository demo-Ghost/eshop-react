import React from "react";

const Home = () => {
  return (
    <div className="m-5">
      <h1>Home</h1>
      <h4>Things missing:</h4>
      <p>Search works only with button</p>
      <p>Mobile hamburger menu</p>
      <p>Order by price with select</p>
      <p>Routing to slugs other than / directly with url</p>
    </div>
  );
};

export default Home;
